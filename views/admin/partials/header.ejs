<%
  const pageTitle = typeof title !== 'undefined' ? title : (heading + ' · Admin · ' + church.shortName);
%><!DOCTYPE html>
<html lang="en">
<head><%- include('../../partials/head', { title: pageTitle }) %></head>
<body class="admin-body">
<div class="admin-layout">
  <%- include('./sidebar', { activeSection: typeof activeSection !== 'undefined' ? activeSection : '' }) %>
  <main class="admin-main">
    <div class="admin-topbar">
      <div>
        <h1><%= heading %></h1>
        <% if (typeof subheading !== 'undefined') { %><p class="admin-topbar-sub"><%= subheading %></p><% } %>
      </div>
    </div>
    <div class="admin-content">
